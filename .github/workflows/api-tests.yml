name: Postmate API Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  run-collection:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Postmate CLI
        run: npm install -g @postmate/cli

      - name: Run School collection (QA)
        run: pmc run ".postmate/collections/School.json" --env QA

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: postmate-report
          path: .postmate/reports/
